/*
Biblioteca muito util para serialização de dados, e persistência de dados

localStorage - Variavel retorna referência a uma base de dados local da pagina
localStorage.setItem(key, value) - Adiciona valora  base de dados local
localStorage.getItem(key) -> Retorna valor da base de dados local

json.stringify(objetojs) -> stringJson - Converte um objetojs para stringJson
json.parse(stringJson) -> objetojs - Converte uma stringJson em um objeto js


json.dump(objetopy) -> Converte objeto python em objeto json
lista = json.load(objetojson) - Converte objetoJson em objeto python

json.dumps(objetopy) -> Converte objeto python em string json
lista = json.loads(stringJson) = Converte stringJson em objeto python


*/
