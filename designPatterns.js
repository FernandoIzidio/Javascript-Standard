/*
CRUD(CREATE READ UPDATE DELETE)
rpa(robotic process automation) - python
mvp(model view presenter)
mvc(model view controller)



Singleton é um padrão de projeto onde os classes de um módulo possuiem uma única instância/objeto.

Caracteristicas do padrão de projeto singleton:
    - constructor privado para não permitir criar mais intâncias da classe
    - metódo publico estatico que vai permitir acesso a única instância da classe
    - instância única

*/
